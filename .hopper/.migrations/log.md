# Migration log

Appended to by `hopper-dispatch --migrate-config`. Append-only — entries are never rewritten.

## hopper v0.48.1 (previous stamp: 0.48.1)

- applied `task-frames` — Add task-type frames this plugin ships but the project does not have
